---
import '@/styles/globals.css';

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="flex grid-header justify-center pt-3">
    <h1 class="border-b-8 border-dashed text-5xl">{ title }</h1>
</header>